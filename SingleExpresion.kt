fun handleTambah(x: Int, y:Int) : Int = x + y;
fun handleKurang(x: Int, y:Int) : Int = x - y;
fun handleKali(x: Int, y:Int) : Int = x * y;

fun main(){
      val resultTambah: Int =  handleTambah(20,10);
      println(resultTambah)
}